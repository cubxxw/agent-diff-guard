# agent-diff-guard — AI 分析(可选)配置示例
#
# 复制为 .env 并填入你自己的 key。.env 已被 .gitignore 忽略,绝不会入库。
# 不配 key 时面板照常工作(只读审计),只是没有 AI 摘要/建议功能。
#
# 隐私边界:启用 AI 后,面板只把【去敏元数据】(rule / path / 统计计数)发给模型,
# 绝不发送代码正文、密钥原文、任务原文、diff 内容。详见 README 的隐私说明。

# DeepSeek(AI 整理引擎)。配了这个 key 即自动启用面板的 AI 分析。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-v4-pro
